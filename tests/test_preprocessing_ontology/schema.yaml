user:
    represented_as: node
    input_label: user

jellyfishobservation:
    represented_as: node
    input_label: jellyfishobservation

reported_by:
    is_a: relation
    represented_as: edge
    input_label: reported_by
    source: jellyfishobservation
    target: user

namedzone:
    represented_as: node
    input_label: named_zone

hasforlocation:
    represented_as: edge
    input_label: has%20for%20location
    source: jellyfishobservation
    target: named_zone

