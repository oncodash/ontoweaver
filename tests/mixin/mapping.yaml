subject: variant # Type for each entry (e.g. line).
columns:
    case: # The column having this label.
        to_object: case # The node type to which it is exported.
        via_relation: variant_to_outcome # The edge type with which it is linked to the subject (here, "variant").
    genomicEntity:
        to_object: genomic_entity
        via_relation: variant_to_genomic

    # We do not import "variant_id".
