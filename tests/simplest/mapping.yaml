subject: variant # Type for each entry (e.g. line).
columns:
    fact:
        to_property:
            data:
                - patient
transformers:
    split_columns:
            patient:
                separator: ";"
                to_object: patient
                via_relation: patient_has_variant
