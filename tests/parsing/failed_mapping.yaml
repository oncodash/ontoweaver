subject: variant # Type for each entry (e.g. line).
transformers:
    - map:
          columns:
              - fact
          to_object: fact
          via_relation: variant_to_fact
          to_property:
              - maladie
          for_objects:
              - patient

